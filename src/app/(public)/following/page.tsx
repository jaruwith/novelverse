"use client"; import Link from "next/link"; import { EmptyState } from "@/components/Content"; import { StoryCard } from "@/components/Cards"; import { useRole } from "@/components/RoleProvider"; import { publicStories } from "@/lib/mockData";
export default function Page(){const {role}=useRole();if(role==="guest")return <div className="statePage"><h1>เข้าสู่ระบบเพื่อดูเรื่องที่ติดตาม</h1><p>การติดตามช่วยรวมตอนใหม่และตำแหน่งที่อ่านล่าสุดไว้ในหน้าเดียว</p><Link className="primaryButton" href="/login">เข้าสู่ระบบ</Link></div>;return <div className="container"><div className="sectionTitle"><div><span className="eyebrow">คลังส่วนตัว</span><h1>กำลังติดตาม</h1></div></div><div className="tabs"><button>อัปเดตแล้ว</button><button>เรื่องจบแล้ว</button><button>ทั้งหมด</button></div><section className="section"><div className="storyGrid four">{publicStories.slice(0,4).map(s=><StoryCard story={s} key={s.id}/>)}</div></section><div className="twoCol"><div className="panel"><h2>ข้อมูลตอนล่าสุด</h2><p><strong>ห้องสมุดหลังสายฝน</strong><br/><span className="muted">ตอนที่ 6 · อัปเดตวันนี้</span></p><p><strong>รถรางเที่ยวสุดท้าย</strong><br/><span className="muted">ตอนที่ 4 · อ่านล่าสุดตอนที่ 2</span></p></div><EmptyState title="ตัวอย่างเมื่อยังไม่ติดตาม" description="กด “ติดตามเรื่อง” ที่หน้ารายละเอียด แล้วตอนล่าสุดจะมารวมที่นี่แทนศูนย์แจ้งเตือน" action="สำรวจเรื่องน่าอ่าน"/></div></div>}
