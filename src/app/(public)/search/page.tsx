import { CreatorCard, StoryCard } from "@/components/Cards"; import { FilterBar } from "@/components/Content"; import { creators, publicStories, tags } from "@/lib/mockData";
export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const {q="ห้องสมุด"}=await searchParams;return <div className="container"><span className="eyebrow">ผลการค้นหา</span><h1>ค้นหา “{q}”</h1><div className="section"><div className="tabs"><button>ทั้งหมด 18</button><button>เรื่อง 12</button><button>ครีเอเตอร์ 3</button><button>แท็ก 3</button></div></div><FilterBar/><section className="section"><h2>เรื่อง</h2><div className="storyGrid four">{publicStories.slice(0,8).map(s=><StoryCard key={s.id} story={s}/>)}</div></section><section className="section"><h2>ครีเอเตอร์</h2><div className="creatorGrid">{creators.slice(0,3).map(c=><CreatorCard key={c.username} creator={c}/>)}</div></section><section className="section"><h2>แท็กที่เกี่ยวข้อง</h2><div className="tagRow">{tags.slice(0,6).map(t=><a className="tag" href={`/search?q=${t}`} key={t}>#{t}</a>)}</div></section></div>}
