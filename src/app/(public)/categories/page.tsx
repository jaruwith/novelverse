import Link from "next/link"; import { categories, publicStories } from "@/lib/mockData";
export default function Page(){return <div className="container"><div className="sectionTitle"><div><span className="eyebrow">สำรวจ</span><h1>หมวดหมู่ทั้งหมด</h1><p className="muted">หนึ่งเรื่องมีหมวดหมู่หลักหนึ่งหมวด และมีแท็กประกอบได้หลายแท็ก</p></div></div><div className="creatorGrid">{categories.map((c,i)=><Link className="panel" href={`/category/${encodeURIComponent(c)}`} key={c}><span className="eyebrow">หมวด {String(i+1).padStart(2,"0")}</span><h2>{c}</h2><p className="muted">{publicStories.filter(s=>s.category===c).length} เรื่อง · ดูแท็กและตัวกรอง</p><span>เปิดหมวด →</span></Link>)}</div></div>}
