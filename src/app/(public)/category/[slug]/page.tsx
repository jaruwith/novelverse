import { CategoryTabs, FilterBar } from "@/components/Content"; import { StoryCard } from "@/components/Cards"; import { categories, publicStories } from "@/lib/mockData";
export function generateStaticParams(){return categories.map(slug=>({slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const name=decodeURIComponent(slug);const matched=publicStories.filter(s=>s.category===name);const shown=matched.length?matched:publicStories;return <div className="container"><span className="eyebrow">หมวดหมู่</span><div className="sectionTitle"><div><h1>{name}</h1><span>{matched.length || publicStories.length} เรื่องทั้งหมด</span></div></div><CategoryTabs/><section className="section"><FilterBar/></section><div className="storyGrid four">{shown.map(s=><StoryCard key={s.id} story={s}/>)}</div></div>}
